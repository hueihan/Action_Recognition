ap

% for training/testing on WIS

setting_file = 'setting_WIS';
feature_computation(setting_file);
acc = classification(setting_file);


 % for training/testing on HMDB
%setting_file = 'setting_HMDB';
%feature_computation(setting_file);
%acc = classification(setting_file);

